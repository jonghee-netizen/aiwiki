---
category: ax-project
title: 캐시 파일 목록
description: NAS Video Quick Searcher - 캐시 파일 목록
tags: [NAS, AX-project]
---

# 캐시 파일 목록

모두 `C:\claude\나스 소재 찾기\` 루트에 위치

| 파일명 | 용도 | 브랜드 |
|--------|------|--------|
| `index_cache.json` | 메인 NAS 파일 인덱스 | 전체 (메인 페이지) |
| `highperf_cache.json` | 고효율소재 엑셀 파싱 결과 | 샤르드 |
| `highperf_triconics_cache.json` | 고효율소재 엑셀 파싱 결과 | 트리코닉스 |
| `highperf_iom_cache.json` | 고효율소재 엑셀 파싱 결과 | 이옴 |
| `highperf_lingtab_cache.json` | 고효율소재 엑셀 파싱 결과 | 링탭 |
| `highperf_drpae_cache.json` | 고효율소재 엑셀 파싱 결과 | 닥터빼 |
| `highperf_soltis_cache.json` | 고효율소재 엑셀 파싱 결과 | 솔티스 |
| `highperf_hwainica_cache.json` | 고효율소재 엑셀 파싱 결과 | 화이니카 |

## 캐시 재생성 시점

- 앱 최초 실행 시 캐시 없으면 자동 생성 (`auto_parse_highperf`)
- 엑셀 파일 업데이트 후 캐시 삭제 → 앱 재시작하면 재생성
